# 🔧 修正 404 錯誤 - 更新檔案

## 問題說明

手機安裝 PWA 後出現 404 錯誤，是因為檔案路徑設定不正確。

## ✅ 已修正的檔案

請重新上傳以下 3 個檔案到 GitHub：

### 1. manifest.json
- ✅ 修正 `start_url` 為 `/boiler/`
- ✅ 版本：v1.0.1

### 2. service-worker.js
- ✅ 修正快取路徑為絕對路徑 `/boiler/...`
- ✅ 版本號更新為 v1.0.1

### 3. boiler-dashboard-simple.html (或 index.html)
- ✅ 修正 service-worker 註冊路徑
- ✅ 修正 manifest 連結路徑

---

## 📤 更新步驟

### 1️⃣ 下載修正後的檔案

從 `/mnt/user-data/outputs/` 下載這 3 個檔案：
- `manifest.json`
- `service-worker.js`
- `boiler-dashboard-simple.html`

### 2️⃣ 上傳到 GitHub

1. 在 GitHub 倉庫中，點擊對應的檔案
2. 點擊右上角的 ✏️ 編輯按鈕
3. 刪除舊內容，貼上新內容
4. 或者直接刪除舊檔案，重新上傳新檔案

### 3️⃣ 確認檔案名稱

⚠️ **重要**：請確認主頁面檔案名稱為 `index.html`

如果你上傳的是 `boiler-dashboard-simple.html`，請改名為 `index.html`

### 4️⃣ 等待部署

- GitHub Pages 會自動重新部署
- 等待 1-2 分鐘

### 5️⃣ 清除手機快取

**Android：**
1. 長按 APP 圖示
2. 選擇「應用程式資訊」
3. 點擊「儲存空間」
4. 點擊「清除資料」
5. 刪除 APP，重新安裝

**iPhone：**
1. 長按 APP 圖示
2. 點擊「移除 App」
3. 重新用 Safari 安裝

---

## 🧪 測試

更新後，測試以下項目：

- [ ] 電腦瀏覽器可以開啟
- [ ] 手機瀏覽器可以開啟
- [ ] 手機可以安裝到主畫面
- [ ] 安裝後點擊圖示可以正常開啟（不是 404）
- [ ] 登入功能正常
- [ ] 離線時仍可開啟

---

## 📋 修正內容詳解

### manifest.json
```json
// 修正前
"start_url": "./boiler-dashboard-simple.html"

// 修正後
"start_url": "/boiler/"
```

### service-worker.js
```javascript
// 修正前
const urlsToCache = [
  './boiler-dashboard-simple.html',
  './qr-mobile-multilang.html',
  ...
];

// 修正後
const urlsToCache = [
  '/boiler/',
  '/boiler/index.html',
  '/boiler/qr-mobile-multilang.html',
  ...
];
```

### boiler-dashboard-simple.html
```javascript
// 修正前
navigator.serviceWorker.register('./service-worker.js')

// 修正後
navigator.serviceWorker.register('service-worker.js')
```

---

## 🎯 為什麼要這樣改？

**GitHub Pages 的 URL 結構：**
```
https://bava-chang.github.io/boiler/index.html
                              ^^^^^^^ 這是你的倉庫名稱
```

所以：
- 絕對路徑要包含 `/boiler/`
- 相對路徑可以直接用檔案名稱
- `start_url` 必須使用絕對路徑

---

## 🆘 如果還是有問題

1. **檢查檔案名稱**
   - 確認主頁面是 `index.html`（不是 boiler-dashboard-simple.html）

2. **檢查所有檔案都已上傳**
   ```
   ✅ index.html
   ✅ qr-mobile-multilang.html
   ✅ manifest.json
   ✅ service-worker.js
   ✅ icon-192.png
   ✅ icon-512.png
   ```

3. **清除瀏覽器快取**
   - Chrome: Ctrl+Shift+Delete
   - Safari: 設定 → Safari → 清除瀏覽記錄

4. **重新安裝 APP**
   - 完全刪除舊的 APP
   - 重新從瀏覽器安裝

---

## 📞 快速檢查清單

更新後請確認：

- [ ] GitHub 上的 3 個檔案已更新
- [ ] 主頁面檔名是 `index.html`
- [ ] 瀏覽器可以正常開啟
- [ ] 手機已刪除舊 APP
- [ ] 重新安裝新 APP
- [ ] 新 APP 可以正常開啟（不是 404）

---

**完成這些步驟後，404 錯誤應該就解決了！**

最後更新：2024-12-03
版本：v1.0.1 (修正 GitHub Pages 路徑問題)
